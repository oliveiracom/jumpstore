import React from 'react';
import './filters.scss';

export default class Filters extends React.Component {
    render() {
        return(
            <div id="filters">
                <h4>FILTRE POR</h4>
            </div>
        );
    }
}